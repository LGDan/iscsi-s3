# Prometheus scrape config for docker-compose integration stack.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: iscsi-s3
    static_configs:
      - targets: ["iscsi-s3:9090"]
        labels:
          service: iscsi-s3
